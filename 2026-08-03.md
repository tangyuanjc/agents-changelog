# 2026-08-03

## [2026-08-03 05:35 CST] [Codex-CTO] [type:ops] Persist the WS-2868 host storage safeguards and deployment boundary

- Scope: WS-2868 / PR #6281 delivered host-side storage safeguards after three recent memory-and-disk saturation incidents; no organization constitution file was changed.
- Source control boundary: PR #6281 is open with green CI. Its Multica daemon admission endpoint is not merged or deployed, and the running daemon was not restarted while active work remained.
- Live host controls: the unused WS-2512 shadow observer is disabled; the 25/18 GiB two-level guard, single-owner retention/GC path and capacity sampler are installed on the host.
- Data-loss boundary: the production-lineage canary passed with a nested three-file fixture, but archival remains dry-run with `archive_enabled=false`, `delete_source=false`, zero archived workspaces and no source deletion.
- Capacity truth: Cursor was inventoried read-only and the 48-72 hour growth window is incomplete, so remaining-days capacity stays `INCONCLUSIVE`.
- Verification: 40 Python fault/transaction/capacity tests, the daemon package and complete `cmd/multica` test binary passed; PR CI and three adversarial reviews passed.

## [2026-08-03 05:36 CST] [Codex-CTO] [type:fix] Pace the Xinxin Codex relay while its target host is offline

- Scope: changed only `~/Library/LaunchAgents/com.jc.xinxin-codex-relay.plist`; no relay payload, credential, destination or organization rule changed.
- Failure mode: the relay is healthy enough to launch, but the target host remains unreachable over SSH and exits 255 after connection timeout.
- Mitigation: set `ThrottleInterval=300` so launchd spaces retries by five minutes instead of repeatedly hammering an offline target.
- Live readback: launchd reports `minimum runtime = 300`, `KeepAlive=true`, `RunAtLoad=true` and the service in scheduled-retry state; the latest stderr window still contains only connection timeouts.
- Remaining boundary: this reduces retry pressure but does not restore delivery. The target machine or network path must return before a real end-to-end relay smoke can pass.

## [2026-08-03 10:40 CST] [Opus-CSO] [type:governance] v2.9 review — 30 天 review 节点补做 (逾期 22 天), 三子项逐条结论

义务 `v29-30day-review` (AGENTS.md:813, v2.9 立于 2026-06-12, due 2026-07-12)。哨兵 WS-2876 催办, 本条即 evidence_pattern `v2.9 review` 的命中锚。三子项结论如下, 数字均为 CSO 本次亲测 (非转述子代理)。

### 子项 1 · v2.9 hard rule 13 (回收层时效规则 + 验收分层) 稳态 → **未稳态, 但半数议题已被 hr37 取代**

- **验收分层 (子规则 1) = 已被 hr37 取代, 停止单独催办。** 7/28 五层全量审计 (wf_cc591544) 用更强口径实测同一问题 (Sol 7 天 0 次独立 PASS/FAIL、131 张 in_review 中 60 张零介入、血统闸 5 维中 3 维失效), 并驱动 hr37 立法: 病灶在**判据形态**不在执行体, 处置 = 机械断言块 + 机械章旁路。hr13 原设想的"独立对抗验收 Role 跑 L3 + CSO 抽查 10%"已被 hr37 ②③ 的更强形态承接。
- **时效规则 (子规则 2) = 三条闸全部无机器执行体, 未落地。** 实测 (上海 2026-08-03 10:40, 全量分页读取):

  | 队列 | 总数 | >72h 无动作 | >14d | >30d | 最老 |
  |---|---|---|---|---|---|
  | in_review | **153** | 69 (45%) | 0 | 0 | 5.3d WS-1694 |
  | todo | 92 | 78 | 51 (55%) | 23 | 82.5d WS-44 |
  | in_progress | 46 | 33 | 16 | 5 | 56.5d WS-392 |
  | blocked | **188** | 176 | 81 | 36 | 67.3d WS-280 |

  未关票合计 **479**。in_review 对照 hr13 立法时基线 (6/10 = 43) = **3.6x**; 对照 7/28 审计基线 131 = 6 天 +22。

  **但根因不是"规则没落码"—— 是落了码没人消费 (本次最有价值的一处自我纠正)**: `metrics/wip_self_close_scanner.py:20-25` 的 `AGE_THRESHOLDS` **恰好就是 hr13 的三条阈值** (`in_review: 3d` = 72h / `todo: 14d` / `blocked: 7d` ≈ 每周重判), 每日 03:15+18:15 跑, 当前产出 **250 条 `decay_proposals`** 写进 `metrics/wip_control_latest.json`。断链在下游: 全 `~/.org` 检索, 除 scanner 自身与其测试外**零代码读 `decay_proposals` / `wip_control_latest.json`**, 且 `auto_close_enabled` 在 `wip_self_close_scanner.py:412` **写死 `False`** (连配置项都不是)。
  **故 hr13 时效规则真实状态 = 检测到位, 执行断链**, 而非"无代码闸"。这与 JC 2026-08-02 的判断同形 —— 「黑板架构体系里面的最大问题是消费端」: 不是没测量, 是测了没人接。正确处置**不是再加一个扫描器**, 是给既有的 250 条提案接一个消费端。
- **巡检 autopilot (子规则 3) = 部分。** `multica-dispatch-sweeper` (每 2h) 与 `org-wip-self-close-scanner` (每日 03:15/18:15) 在跑且**分页正确** (`wip_self_close_scanner.py:75-94`、`m3_daily_snapshot.py:250-258` 均按 `has_more`/`offset` 翻页 — 代码侧无截断问题, 截断只发生在 agent 即兴敲 CLI 时)。缺的是规则点名的两项: in_review 年龄分布摘要、死锁票检测 (依赖图指向 done/cancelled 的 blocked 票)。
- **closing 队列收敛 (子规则 4) = 无法核实**, 无日志证据面, 不拼结论。

### 子项 2 · Role Phase2 写入边界是否论证 → **未论证; 本次拍板冻结为无期限 backlog, 不再催办**

- 检索零命中: agents-changelog 全量 / CSO memory 全库 (含 MEMORY-archive.md 冷层) / `~/.org` 全目录 (含 plantree) / Multica 票面全量 —— 四面均 **0 条** Phase2 写入边界论证记录。
- 但**议题未被架构废除**: AGENTS.md:440 与 :452 (v3.10, 2026-07-20, 制图员 Role 与 CC/OG 入册) 两处主动写「登记进 `~/.org/roles/` 注册表 (**与 Role Phase2 live 挂载解耦, 先登记后挂载**)」—— 组织已用"先登记后挂载"的解耦模式绕开了 Phase2 决策点。
- 机制实测: `~/.org/roles/.mounts/` mtime 2026-07-17 05:12 CST (17 天无写入), 内容仅 `memory_refs/sentinel-role.yaml`, **无任何真实 live mount**。
- **判定**: 不是"论证欠账", 是**议题已被更省的方案绕过, 54 天用脚投票**。按最小工程铁律, 补做一次无消费者的论证 = 纯浪费。冻结, 宪法原文标注, 不阻塞注册表模式。若将来 runtime 真需要直接消费 `.mounts/` 再解冻。

### 子项 3 · 哨兵泛巡检是否再漏 → **部分补上: 从"零检测"进步到"检测到但降噪进日摘要"; 仍缺产出物维度**

- **当事 job 复测** (`ai.hermes.aihotboard-rsync`, 就是 6 月"断 3 天零告警"那一个): 今日 121 次运行 / 11 次失败 (9.1%), 历史 3511 / 289 (8.2%)。**性质与 6 月不同 —— 是间歇抖动 (VPS SSH connection closed) 且每轮自愈, 非持续断**; 最近一次成功 10:37:31, 产出物新鲜, 未构成事故。
- **告警链路已通但被降噪**: `multica-runtime-offline-alert` 确实检出 (日志 10561 次命中), 但 `route=digest`、`suppressed_count=29359`、`flap_edges=2`, 即时告警被抑制、只进每日摘要 (今日 09:59 摘要含 14 条已发出)。对间歇抖动这是**正确**的降噪; 风险点在于它标了 `confirmed persistent failing` 仍走 digest —— 真持续断时会埋在 14 条摘要里。
- **剩余结构缺口 (本次最刺眼一条)**: 现有巡检判据几乎全是 **launchd 退出码维度**, 缺 **产出物新鲜度维度** (没有任何东西检查 VPS 上目标文件的 mtime)。6 月教训的真正形态 = "服务 loaded 着但产出停了", 退出码维度天然看不见这一类。
- **哨兵自身即活体样本**: 开本票的宪法义务哨兵报「in_review 100」, 真值 **153**。根因 = `multica issue list` 单页硬上限 100 且**静默截断** —— 实测传 `--limit 500`, 返回体 `limit` 字段回显 500、`issues` 数组只有 100、`has_more=true`、`total=153`。todo(92)/in_progress(46) 因低于 100 未受影响, 故只有 in_review 一个数字错, 极难肉眼发现。**读 `len(issues)` 而不读 `total` 的调用方都在无声地少看**, 且漏掉的恰好是 hr13 要监控的那个指标。

### 处置 (本次已执行, 非待办)

1. 本条即 changelog 收口, 义务 `v29-30day-review` 在 `~/.org/metrics/constitution_duty_calendar.json` 转 `retired_duties`, 停止重复催办。
2. AGENTS.md:799 Role Phase2 段追加冻结注; hard rule 13 段追加 review 结论指针。
3. 哨兵 autopilot `8247bbc6` prompt 已当场补「读票必分页」硬段 (CSO 直接改, 无需派单) —— 截断不在代码里, 在 agent 即兴敲 CLI 的路径上, 且它威胁的是哨兵自己「不重复开票」的幂等边界。
4. 派 CTO Codex **WS-2878** (WS-2876 子票, 带 hr37 ① 六条机械断言块): 给既有 250 条 `decay_proposals` **接消费端** + 补死锁票检测 + 给 watchdog 加**产出物新鲜度维度** (首个实例接 rsync, 注明是补监控维度非修故障)。这是唯一需要写代码的一项 —— 其余均为治理动作, 本次当场完成。

## [2026-08-03 11:21 CST] [Codex-CTO] [type:feat] Connect hr13 decay proposals and add artifact freshness paging

- Scope: WS-2878 / org-constitution PR #34; no organization constitution rule changed and automatic issue closure remains disabled.
- Decay consumer: the existing fully paginated WIP scanner now posts one Shanghai-day summary comment to the original WS-1708 ledger, keyed by an HTML marker so repeated same-day runs do not duplicate delivery.
- Summary evidence: action counts, status-over-threshold counts, full in-review age distribution, top-ten oldest proposals, and top-level `deadlock_detection` for blocked dependencies that are done, cancelled, or missing.
- Freshness dimension: the centralized host watchdog now accepts `{job_label, artifact_path_or_probe, max_age_minutes}` declarations; `artifact_freshness:*` bypasses the warning digest and pages directly.
- First live declaration: `ai.hermes.aihotboard-rsync` probes the VPS `hotboard.sqlite` mtime under `/var/lib/ai-hotboard/.hermes/data`; the rsync producer and its 300-second schedule were not modified.
- Verification: 84 tests pass; the live-sized scanner found 249 proposals and 7 deadlock edges; WS-1708 comment count was `3 → 4 → 4`; a 1-minute fault injection emitted `artifact_freshness:ai.hermes.aihotboard-rsync route=jc` while the production 15-minute threshold remained healthy.
