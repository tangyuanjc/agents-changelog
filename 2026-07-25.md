# 2026-07-25 agent changes

## 宪法 v3.13 · hr35 HTML 双端送达 + 专属 bot 通道 (Opus-CSO, 03:50 Asia/Shanghai · JC 2026-07-24 02:19 指定升格)

- **触发**: JC 2026-07-24 02:19 上海在 Codex 线程 019f88bb 发现 CTO 交付的多份 HTML **只在 Codex Desktop 右侧面板显示**, 飞书端拿不到, Remote Control / 手机场景等于没收到。JC 当场对 CTO 立三条本机规则, 并明确指定 "等后续 CSO 恢复限额了, 我会让它把这个规则升级为全组织 agents 的通用铁律"。CSO 限额恢复后按 CSO 自主权铁律直接执行三连, 不回问。
- **规则内容 (hr35, 五条)**:
  1. **展示 ≠ 送达** — 任何 agent 在自己宿主界面 (Codex Desktop 面板 / Claude Code 终端 / CLI 输出 / 本地路径) 展示 HTML, 一律不计交付完成。
  2. **双端强制同步** — 最终 HTML 展示的同时必须推收方飞书, 以返回 `message_id` 为唯一送达凭证, 无 message_id = 未送达。
  3. **专属通道不混用** — 每个 agent 用自己的飞书 bot 身份 (Codex→`lark-codex` Codex bot / Opus-CSO→自己的 bot / 艾伦→自己的), 禁借用他人 bot 代发。与 hr19 补丁 (禁 agent 借 JC 身份签验收) 同源: **发信身份必须等于实际产出者**。
  4. **fail-closed** — 推送失败不得静默, 不得以宿主界面展示冒充双端完成; 票面/回复必须写失败原因 + 重试入口 (承接 hr26 fail-loud)。
  5. **适用面** — 所有 agent (C-level / worker / Role / 员工机 agent) × 所有人类收方; 敏感内容只私聊不发组织群 (承接 hr27 ACL); agent↔agent 中间产物仍走 Markdown, 不触发。
- **与 hr20 的分工**: hr20 管 "给人类默认出 HTML" (**产出层**), hr35 管 "什么才算送到" (**送达层**)。hr20 原文已有 "产出后 lark-cli 推飞书" 一句, 但只是描述不是硬闸, 且未规定 bot 身份归属 — 实证 (CTO 一周多份 HTML 只在面板) 证明描述性表述对机器零效力, 与 hr28 教训同源, 故独立成条。
- **AGENTS.md 修改 (3 处)**:
  - Line 366 baseline v3.12 → **v3.13**
  - Line 369 版本演进列表末尾追加 v3.13 条目
  - 新增 `**hard rule 35 ...**` 段, 插在 hr34 之后 / `## 🚨 hr28` 章节之前
- **凭证**: `~/.org/AGENTS.md` 2305 行; `grep -c "hard rule 35"` = 1; `grep -oE "baseline = \*\*v3\.[0-9]+\*\*"` = `v3.13`。
- **CTO 侧已先行 (不重复做)**: Codex 本机执行票与验收标准见 `Documents/New project 3/docs/multica/codex-html-dual-delivery-policy-2026-07-24.md`; agents-changelog `4c0d445 [Codex-CTO] enforce HTML dual-end delivery via Codex bot` 已 push。本次 CSO 只补**全组织宪法层**, 不改 CTO 已落地的本机实现。
- **不做/边界**: 不为本条新建 loop 或 autopilot (违反 hr29③ 与 hr32 最小工程); 不改 hr20 本体; 不追溯已发出的历史交付。落地靠各 agent 自检 + 收方 (JC/员工) 现场反馈, 不设监控层。
