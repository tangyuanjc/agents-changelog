
## 2026-05-04 02:35 [Opus-CSO] 黑板架构 v2.4 patch · Layer 4 拆双轴

**类型**: 跨 agent 铁律变更 (三连: AGENTS.md + changelog + push)

**变更**:
- Layer 4 MEMORY 从单数 "GBrain 第二大脑" 拆成:
  - **4a · World Library** (GBrain 知识库, 静态 markdown corpus)
  - **4b · Agent Experience** (Hindsight 经验记忆, 动态 runtime + reflect)
- 宪法 3 处修订: line 15-18 基础设施定义 / line 254 七层结构表 / line 272 GBrain 段整体改写

**为什么**:
- 3 路 sub-agent 调研判决: 业内官方都明说互补不替代 (GBrain README "library 不是 memory" + Hindsight 文档 "use both for some teams")
- 当前 Layer 4 dirty state — GBrain 259 pages / Embedded=0 / 0 agents 真激活
- JC 直觉 "好像没真正全量执行" 90% 对 — 数据层 ✅ 应用层 ❌

**配套派单**:
- P0a 修 GBrain (PID 92511) — embed + 19 cron + agent 接入 + 大屏 endpoint 预留
- P0b Hindsight 公司级 (PID 92512) — self-host Docker + 多 agent + ingest + 大屏 endpoint 预留
- P0d 大屏第 8 卡前端 (P0a/b 完后再派)

**30 天 review**: 5 月底 review GBrain v0.30+ 是否 stable / Hindsight 公司级 ROI / 大屏第 8 卡上线

**关联 memory**: project_layer4_dual_axis_0504.md

## 2026-05-04 03:35 [Codex-CTO] P0b Hindsight 公司级 self-host 交付

**派单**: Opus-CSO P0b · Hindsight 公司级共享，目录 `~/.org/projects/hindsight-company-level-0504/`，要求 B1-B5、per-repo commit、HANDOFF。

**交付**:
- B1 审计：小J COO 原配置为 Hindsight Cloud `xiaoj-coo`，当前云端 stats 为 `3827 nodes / 104770 links / 208 documents / 1820 observations`；旧 cloud config 已本地备份，未删除云端数据。
- B2 self-host：Mac mini 安装最小 Docker runtime (`docker`/`docker-compose`/`colima`)，Hindsight v0.5.6 self-host 跑在 `http://localhost:8888` + UI `:9999`，`/health` 返回 healthy。
- B3 多 agent：爱马仕默认、COO 小J、Ogilvy 均接到 self-host `company-shared` bank；`auto_retain=false`，只允许显式 company-level retain，避免私聊/私域自动入库；B3 retain/recall 探针命中 Hermes + COO 两条结果。
- B4 ingest：新增 `~/blackboard-v3/scripts/hindsight-ingest.ts` 和 hourly launchd `com.user.hindsight-ingest`，仅 ingest `~/agents-changelog/`、Paperclip issues/activity、`~/.org/wake-up-brief/*.md`、`~/.org/projects/*/HANDOFF.md`；launchd 手动 kick exit 0，写入 12 个 allowed source docs。
- B5 dashboard：Blackboard v3 新增 `GET /api/nasa/hindsight`，返回 self-host live stats；验证时 `company-shared` 为 `488 nodes / 10092 links / 14 documents / 246 observations`。

**边界**:
- 未 ingest `~/.codex/memories`、`~/.claude` memory、Hermes memories、微信 raw、飞书私聊 raw、日记。
- 未删除 COO 旧 Hindsight cloud 数据；云端仍可回滚。
- 因 privacy boundary，未执行 cloud `original_text` 批量迁移到 shared bank；只记录“技术上可读 export”，待 JC/Opus 批准迁移口径后再做。

**验证**: `curl http://localhost:8888/health` healthy；`bun run start:check` passed；`launchctl print gui/$(id -u)/com.user.hindsight-ingest` last exit 0；Hermes gateway default/COO/Ogilvy 已 kickstart，PID 均变化。

**commit**: `~/blackboard-v3` `ee93cdb [Codex-CTO] add company Hindsight self-host integration`；本条 entry 随 `~/agents-changelog` repo commit 落地。
