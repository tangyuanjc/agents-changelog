# 2026-06-25 (上海时间)

## WS-951 二次独立审查 收口 (Opus CSO, 05:40 CST)

**任务**: Mac mini FlClash/TUN 全机网络配置二次独立审查 (WS-951, JC 6/24 19:44 立, 父 WS-943 已 done)。

**结论**: 全部 L1/L2/L3 baseline 与 Codex 03:40 实测对齐, runtime 健康; **1 项 P1 + 3 项 P2 + 1 项 cosmetic** 待 JC/Codex 择机收敛。CSO 角色只审不改, 已落审计评论 → WS-951 issue 47d2da8f-65fa-4662-8f90-abe30bfcf032。

### 新发现 (本轮加进 memory feedback_flclash_tun_macos_network_setup_0624)

- **P1 · prefs vs runtime `tun.auto-route` 不一致**: prefs(`defaults read com.follow.clash` flutter.config.patchClashConfig)=`false`, generated `config.yaml`=`true`, runtime 用 true 所以现在 OK; 但 FlClash 下次重启若按 prefs 重生成, auto-route 翻 false → utun4 在但默认路由不再走 198.18.0.1 → 透明 egress 死, codex-reliability-proxy / shell-only 流断网。修法: UI 显式拨 ON 或删 patchClashConfig.tun.auto-route 字段。
- **SOP**: 改 mihomo TUN 参数后必查 prefs vs generated config.yaml 一致性, 不一致=重启复发风险。

### 重要现状对齐项 (P1-2/P2-1/P2-2/P2-3)

- Mac mini 仍 `allow-lan: true` + `bind-address: "*"` + `dns.listen: 0.0.0.0:1053`, 与 MacBook baseline `allow-lan: false` 不对称。可信 LAN 内可控但违反最小暴露。
- Ethernet `networksetup -getwebproxy` 仍记 `127.0.0.1:7897`, 当前 disabled 不影响; 修法: `sudo networksetup -setwebproxy "Ethernet" 127.0.0.1 7890` + 同套 SOCKS。
- `launchctl print-disabled system` 仍有 `io.github.clash-verge-rev.clash-verge-rev.service => enabled` override 记录, plist 已删, 纯卫生。
- limactl PID 6197 残留 CLOSED→7897 fd, OS 已断、limactl poll 未回收, inert, 等下次 `colima stop && colima start` 自然清。

### 三连判定 → 二连

- 不动 `~/.org/AGENTS.md`。现有"代理/网络改动验收必须 3 层" + "secret hygiene"铁律已覆盖本次审计方法论。
- 本次发现 = Mac mini ↔ MacBook config drift + prefs/runtime divergence, 都是 **operational SOP 缺口**, 不是新跨 agent 行为铁律。
- 走 changelog + memory 二连足够; WS-953 (CTO Codex 立的 SOP 子票) 走同口径。
- 升宪法的触发条件: 若后续发现 worker agent 把 7897 硬编进新 daemon plist 而绕过"启动前 nc 实测端口"动作, 那时升"daemon 启动前必须 nc 实测代理端口"——现在无此证据, 不立。

### 关联

- 父票 WS-943 (FlClash 迁移, done) / 同期 WS-952 (MacBook 复核, in_review) / 子票 WS-953 (CTO Codex 立的 SOP 审查) / 平行 WS-951 (本次)。
- Memory: `feedback_flclash_tun_macos_network_setup_0624.md` 新增"二次独立审查"段。
- 出差侧支线: CTO Codex 已完成 MacBook 切手机热点的 Tailscale DERP path + UU 远控兜底 (双向 ping/nc 22/真实 SSH 均通); UU 包内 uuyc-cli 1.0.0 可列设备但无 terminal/exec/shell 子命令, AI Agent 自动化触发远控仍需走 SSH。

## 行动项收口

- [x] WS-951 审计 → 评论已落
- [x] memory feedback_flclash_tun_macos_network_setup_0624 增量段
- [x] 当日 changelog (本文件)
- [ ] push GitHub: 下一次 ~/agents-changelog 仓自然 push 时一并带过去 (本次未单独 push)
- [ ] (JC/Codex 择机) P1-1 修 auto-route 一致性; P1-2 收敛 allow-lan; P2-1 Ethernet 端口统一 7890

—— Opus CSO
