# 2026-09-03 (上海时间)

## 宪法 v3.26 · hr35③a 实例标识锚 (Opus-CSO 立 · 验收官 Sol 跨血统打回驱动 · WS-4252)

**背景**: 8/30 Sol 对 WS-4252 做跨血统终验, 8 条通道 + WS-4254 执行项全部 PASS, 并**接受** CSO 那处收敛 (JC 原话「一窗一 bot」→「血统级 bot + 窗口名前缀」), 认定已显式披露非暗缩范围。但 Sol **打回一处**: CSO 把无名窗口 `wV:p3` 判成「不挡事」是错的 —— 两级方案里窗口名是唯一实例级识别字段, 无名 = 该窗口**发不出任何合规消息**, 所以「血统覆盖 100%」推不出「窗口身份覆盖 100%」。

**改动 (hr35③a)**:
1. **实例级权威字段由「窗口名」改为 `pane_id`** (如 `wQ:p5`, herdr 分配, 永不为空 / 永不重名 / 不随改名漂移)。首行格式 `[<窗口名>·<pane_id>]`, 窗口名为空时退化成 `[<pane_id>]` 仍合规。**把「无名窗口」从违规态变成构造上不可能**, 不靠规则去追 —— 直接对齐 v3.21 元教训「加第二条禁令不会让第一条落地」。
2. **据实写明机械覆盖缺口**: 两级身份只有血统级有硬闸 (`~/.local/bin/lark-{grok,kimi,cursor,codex}` 各自 `exec lark-cli --profile <血统>`, 物理够不到 CSO 播报号); **实例级前缀零机械闸** —— 无注入、无空名拒发, 全靠自觉。引用时不得当作已覆盖 (沿用 v3.23 先例: 拒绝把「已写规则」说成「已覆盖」)。

**为什么不改回一窗一 app**: 舰队实测 12 窗口只有 5 个血统, 逐窗建要 12 个飞书 app; 且 Sol 指出多建 app 不等于凭证隔离 (profile 凭证同在一台主机), 真防伪需 credential silo, 不在本票暗扩。

**9/3 实测舰队**: 12 窗口 / 5 血统 —— codex 3 · grok 3 · kimi 4 · cursor 1 · **agy 1**。空名 0 / 重名 0。`wV:p3` 已随舰队轮换消失 (**是消失不是被修**, 结构性成因未动, 故走 pane_id 改造)。
**新缺口**: `agy` 血统 (`agy-gemini` wQ:p5) 无自有飞书通道 —— `~/.lark-cli/config.json` 7 条 profile 无 agy/gemini/anti 命中。该窗当前 idle, 按 ③d 借道显式署名口径处理, 不为 idle 窗口建 app。

**机器凭证**: `~/.org/AGENTS.md` 2544 行 (2537→2544, +7)。8 通道探针 9/3 复测全 `99992351` PASS。
