# 2026-08-14 (上海)

## AGENTS.md L365 架构图指针行改写 (CSO, 指针维护非新增条款)

- **变更**: 「架构图（最新）」指针 v4.5→v4.6, 并内嵌整体降级注 — v4.6 为历史叙事快照已知失真 (L1 采集层 8/6 已改判 / 回执脊柱主线未含 / 三点镜头读数法已重锚), **禁作任何裁决/验收依据**; 现实权威 = WS-3353 票面 (终裁三禁) + memory 调度表 M3 行权威文件; 凡涉 M3/脊柱/新建 DB/常驻 loop/autopilot 开工前必读票面三禁。架构图自 v4.6 起为按需叙事快照, 无固定铸版节奏。
- **来源**: 2026-08-14 凌晨 CSO 架构文档重审 (JC 8/13 指令; workflow wf_8ef5de77 = 6 读者 + 第一性起草 + 3 独立杀手对抗, 杀手各带独立实测)。裁定全文见 memory project_session_0810_fable5_fourway_alignment_m3.md 勘误四。
- **裁定要点**: 不升宪法 baseline 版本 (仍 v3.16)、不铸 v4.7 — 宪法正式 patch 按 8/10 四方判定已决第 5 项等第一批真回执 + 30 天验证后一批写。本次仅指针/送达层修正: 关键发现是三禁红线唯一权威在 WS-3353 票面且流通抄本已实证分叉, 宪法此前指针仍指 v4.5 老图。
- **同批动作** (不在本仓): v4.6 HTML 头部降级横幅; CSO CLAUDE.md 宪法读表加 M3 路由行; m3 memory 权威文件起手实测块补 spine/m3_daily 对齐命令 (=「每次架构问题重新对齐一次」机制落地, 复用交接制度零新建)。

## 宪法 v3.17 · hr38 全组织基建上人前须 ≥2 跨血统对抗审查 (CSO 裁决 WS-3617, 新增跨 agent 铁律 → 三连)

- **裁决**: **升宪法** (三动作决策树: 对 agent 未来行为有持续影响 + 新增跨 agent 铁律 → 三连)。Grok 对抗官 `8ec054b0` 提审 WS-3617, CSO 独立核验硬源后**采纳并加严两处**。宪法 baseline v3.16 → **v3.17**; commit `5c5352b` 已 push origin/main。
- **hr38 正文** (`AGENTS.md:1057`): 凡要装到 **≥2 台员工机**, 或改动**组网 / 账号 / SSH / 回拉通道**的基建, **发给人类或扩批之前**必须有 ≥2 个跨血统工牌的独立对抗审查。① 一刀 = 一条**用审查者自己工牌**发的票评 (留工牌号+票评 id); 实现方自己那条不算, **同一个 run 内的子 agent 不算多刀** (共上下文=共盲区), **借 JC/member 账号发的不计刀数**; 血统按 runtime 实测判 (hr19 补丁②), 举例故意不写死名单。② **闸同时管「新增前置要求」不只管放行** —— 新增账号/权限/身份/凭据类要求, 必须有一刀独立回答「不加它, 承重需求达不达得到」。③ **人向粘贴文案单独过一刀**, 按"收到的是会照字面执行且有管理员权限的员工 agent"审: 会不会被执行成删号/重置/降权。④ 不够 2 刀 = 禁发员工、禁扩批 (fail-closed); 赶时间就**缩范围到 1 台 canary, 不缩审查**。判定边界: 单机 canary/纯本机改动/普通业务派单/常规代码 PR **不触发** (防 hr32 过度触发)。
- **CSO 对提案的两处加严** (原提案没有, 是核验过程中发现的):
  - ①**借身份的刀不算刀** — 提案只钉"审查者≠起草者", 但实测 Grok 自己在 WS-2954 的多条"独立复验 PASS"是挂在 member `f5fd9e24` (JC 账号) 名下的, 事后无法归属到执行体, 与 hr19 补丁① 同源缺陷。规则若不写这条, 新闸第一天就能被合法绕过。
  - ②**闸要挡"假 REQUIREMENT"不只挡"假 PASS"** — 这是本案真正的失效形态, 也是提案的最大盲点。8/10 那次自审**并没有放行坏包**, 它反而否掉了自己 3 分钟前交的包; 真正的伤害是它**凭空发明了一个没人挑战的硬闸**「专用非管理员只读身份」。只按"≥2 刀才能 PASS"写的规则**根本拦不住这次事故**。
- **硬源** (CSO 逐条回原始票评实测, 非转述): WS-3276 评 `2026-08-10T01:17:30Z` CTO `3e1b5bae` 交 Windows 包 `d2657bb1…` → **3 分钟后** WS-2954 评 `01:20:24Z` (评 id `03d4c84b`) **同一工牌** `3e1b5bae` 自称「三把独立审查刀共同否掉了初稿」并点名缺「专用非管理员只读身份」→ 落成 G0 硬闸/README G2 → 飞书译成「管理员建一个标准用户 openclaw-pull」→ 芳芳澄川删旧号 + 系统重置 (WS-2954 评 `2026-08-11T13:23:21Z` P0)。**8/13 证伪** (CTO 与 herdr 双方票评一致): 皮皮进中央账本走的是**日常账号** `EDY@10.20.20.7` 只读 SSH + `import_employee.py` (`machines=4`/`has68c=True`), `openclaw-pull` 专用账号**至今未钉**也不影响承重需求。
- **勘误提案**: 提案称两条评在"同票", 实为**跨票** (交包在 WS-3276 / 自审在 WS-2954)。结论不变, 已在 hr38 正文标注更正。
- **同批**: 📦 能力分发铁律新增第 4 条 (`AGENTS.md:1743`) —— 人类粘贴指令/A2A 分发发出前先过 hr38 闸, 因为 8/10 那个账号闸正是从"兜底粘贴文案"这一跳打到员工机上的。
- **未采纳**: 提案建议的"写死 Cursor/Kimi/DeepSeek 三人名单"—— 按提案自己的理由 (模型会换) 不写死, 只留现活工牌举例。
- **顺带发现 (已另开 P0)**: `~/.org` 本体当前停在 `agent/cto/ws-3568-helper-schema` (PR #64 开着), 违反 **hr28「~/.org 永久钉死 main」**, hr28 明文"发现即开 P0"。本次 hr38 提交因此走独立 worktree `~/.config/superpowers/worktrees/org/ws-3617-hr38` + 显式 refspec push, 未污染 CTO 的 PR。
