# 2026-08-25

## [Opus-CSO] 宪法 v3.21 — hr35③ 补丁 · Agent↔飞书 Bot 一一对应落地闸

- **时间**: 2026-08-25 15:41 上海。驱动票 WS-4252 (JC 立项)。
- **判断**: JC 要求"把 Agent↔Bot 一一对应写成宪法铁律"。实测发现**这条铁律 2026-07-24 已存在 = hr35③**, JC 当时原话就是"必须使用你自己的 CLI 通道, 不要混用 Opus 的 CLI bot"。一个月后仍在混用 → 缺的不是条文, 是**判据 / 罚则 / 机器自检**。故走 **hr35③ 补丁**而非新立 hr41 (避免重复条款, 对齐 hr32 最小工程)。
- **补丁四条**:
  - **③a 两级身份**: bot = 血统级 (一 runtime kind 一 app, 禁跨血统借用); 窗口名 = 实例级 (同 bot 多窗口时消息首行 `[窗口名]`)。**这是对 JC 原话「每个 Agent 窗口严格对应自己的 bot」的一处 CSO 收敛** — 实测 kimi 血统同开 3 窗, 逐窗建 app 需 3 次建号+申 scope+管理员审批+版本发布, 违反 hr32; 两级身份保住"可唯一溯源"这个真实诉求, 成本近零。已在正文显式标注供 JC 推翻。
  - **③b 违规处理口径** (JC 明确要求补): 借用他人 bot = 流程违约, 与 hr19「借 JC 身份签验收」同级。三件处置 = 当场更正来源 + 自有通道补发 + 记 changelog。**冒用 CSO 播报号 `cli_a94d194592f8dbb7` 加重**: 当日停该 agent 对外发送权, CSO 复核后恢复。
  - **③c 上线前机器自检** (对齐 hr37): 禁凭 `auth status: ready` 判定能发。探针 = 用无效 open_id 打 `POST /open-apis/im/v1/messages`, 回 `99992351` = scope 齐, 回 `99991672` = 缺 scope。
  - **③d 过渡口径**: 借道必须首行显式署名 `[真实 agent 名 · 经 X 通道临时代发]` 并同开修复票; 无署名借道不因"临时"豁免。
- **实测盘点 (8 通道 × 6 窗口, 探针法)**: 7 条通道 scope 齐 — CSO `cli_a94d1945` / grok `cli_aad98b75` / cursor `cli_aae2d248` / cto-codex-multica `cli_aabef812` / lark-codex `cli_a9480955` (独立 config dir 非 profile) / ogilvy `cli_a93f952b` / yeshou-fahuo `cli_aada40ef`。**唯一坏的 = kimi `cli_aaf03172`**, 缺 `im:message:send` / `im:message` / `im:message:send_as_bot`, 拖垮 kimi 血统全部 3 个窗口 (kimi-x / boss-cu / selfcollect-hn)。**agy-gemini (Antigravity 血统) 无任何通道**。
- **修正一处旧认知**: `auth status` 对全部 8 条通道均返回 `ready`, 其中 kimi 实发被拒 — ready 只证明 token 活, 不证明 scope 齐。这是把 ③c 写成硬闸的直接原因。
- **落地派单**: Kimi 补 scope + agy-gemini 通道评估已派 CTO Codex (WS-4252 子票), 按 hr40 派单锚以父票 in_progress 持有看护。
- **边界**: hr35 原文①②④⑤⑥ 未动; 未新建任何 hr 编号; 未改任何 bot 配置或 scope (那是 CTO 执行面)。
