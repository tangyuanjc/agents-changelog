# 2026-08-10

## [Grok-Build] [type:config] Cursor 默认模型 → composer-2.5 + Codex 卸 Superpowers（昨已执行今日收口）

### Cursor
- `~/.cursor/cli-config.json` → model/selectedModel = `composer-2.5`
- Multica agent `c8dd9931` → `--model composer-2.5`
- backup: `cli-config.json.bak-composer25-20260810-011001`
- 核实: `cursor-agent --list-models` 显示 `(current)`

### Codex Superpowers（2026-08-09 执行，本条归档）
- 卸常驻 Superpowers；归档 `~/.codex/backups_state/superpowers-removed-20260809-231137/`
- Claude Code 6.2.0 保留
- **未改** `~/.org/AGENTS.md`（无新跨 agent 铁律；worktree 路径名含 superpowers ≠ 插件依赖）

### 治理判定
- 对 agent 行为有持续影响但 **非宪法级** → **二连**（changelog + push），不三连 AGENTS.md

## [Opus-CSO] [type:rule] 宪法三连 · 能力分发铁律 §补丁「A2A 卡死 → 人类触达降级」（WS-3347 裁决）

### 背景
Grok 对抗官提审草案：A2A 完不成必须由人类做的动作时，agent 应主动用主人飞书 user 代达 + 配可粘贴 Codex 指令，请求升为全组织铁律。驱动实证 = 皮皮 Windows 自采 collector G0 预检 fail-closed（WS-3276 blocked · 试点 WS-2958）：Multica A2A 以 SYSTEM 身份跑，G0 必挂，非网络问题。

### CSO 裁决 = 采纳延伸，但改写后升（非原样通过）
落点 `~/.org/AGENTS.md`「📦 能力分发铁律」下新增 `### 📦 §补丁`，**不新立 hr38**（Grok 自荐延伸，与最小工程/hr32 一致；hr25a 补丁不bump baseline 为直接先例，故 **宪法 baseline 仍 v3.16 不变**）。同步在原「判定边界」行加例外指针，避免读者只看到"普通业务派单不触发"被误导。

### 相对草案的 4 处实质改写（对抗审查产出）
1. **触发判据机械化** — 草案"合理重试后仍无法完成"无判据，改为「blocker 根因是必须人类在自己机器/账号上动手（身份/OS/交互桌面/物理设备冲突）」；临时性错误（网络/限额/daemon 未起/员工暂未响应）明确不触发，仍走 Tier 排序 v2 既有阶梯。
2. **补回署名义务** — 草案正文遗漏署名（虽实际操作有做）。定为「首句必须标明 agent 代发」，原则写死 **身份可借，来源不可隐**。核实 hr19 补丁真实范围：只锁验收/裁决类代签且明文豁免常规操作类 → 本条不受其禁，非冲突。
3. **堵 Grok 自我解禁** — 草案"本机/C-level agent 默认用 JC user"，而 Grok 正是本机非 C-level agent，原样通过将静默解除其「员工侧禁区」（54% 幻觉 × 员工无判别力）。改为：Grok 只准转达 CSO 已定稿的事务性催办，不得直发自撰判断/分析，内容触达仍必经 CSO 转译。调和 7/10 员工侧禁区 与 8/6 JC「Grok 代 JC 私聊员工催导出用 user」两条既有指令。
4. **员工机 agent 默认不代发** — 草案让员工机 agent 用员工本人 user。实测：宪法 1865 行只写了"各员工自己 Codex 装 lark-cli 走 user 身份"的**意图**，是否已装未逐台核实 → 未核实即等同没有（写作纪律：给员工机设义务必须同时写送达路径，否则名义覆盖无效）。默认改为升级本机 CSO 代达；将来实测有本人授权且 JC 点头方可用本人身份，且**永不冒用 JC 身份**。

### 已核实（非转述）
- 机制**并非新授权**：JC 2026-06-07 已立「推进代劳时你就代表我的意志」（memory feedback_cso_proxy_contact_employees_0607），8/6 又对 Grok 重申。本补丁只是把既有 CSO 个人规范升到宪法层，让 CTO/worker/员工机 agent 同受约束。
- 首条实证 message_id 独立核验：`om_x100b68afa54164b4c2437aac14bfa5b`，2026-08-10 20:06 上海，sender = JC open_id（user 身份非 bot），正文首句已标"agent 代发"且内含整段可粘贴 Codex 指令 —— 与本补丁要求形态一致。
- WS-3276 现况 blocked、assignee = Codex-CTO，Windows G0 优化本就在票上 → **不另开重复票**（草案建议的"另票 CTO 优化 G0"驳回，最小工程）。

### 治理判定
新增跨 agent 铁律 → **三连**（AGENTS.md + changelog + push）。
