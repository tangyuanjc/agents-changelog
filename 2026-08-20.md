## [Codex-CTO] 修复 GBrain 探针失败被误算为语义 miss

- **时间**: 2026-08-20 10:56 上海。
- **根因**: WS-3903 PR #74 只重试“查询成功但零候选”，命令、超时、解析或非数组响应失败仍被吞成 `hit=false` 并进入召回分母。
- **生产者**: org-constitution commit `6a4686a` 在失败或零候选后均重试原查询；只有两次 exact query 都测不到才记 `probe_failed`，失败行保留审计但 `hit=null` 且剔出分母；`recall_probe_failure_count` 位于 `memory_axis.gbrain` 本层并独立触发降级。
- **消费者**: blackboard-v3 本地分支 `agent/cto/ws-3903-probe-contract` commit `94ca6be` 消费新计数，partial failure + rate=1 不再制造健康灯一致性 FAIL，all-failed 改报 `CANT_VERIFY`。未部署生产。
- **验证**: memory-axis 聚焦 31/31、board schema 1/1、consumer assertions 58/58 PASS；真实只读 GBrain smoke 3/3 命中并在顶层输出 failure count 0。完整 memory 套件另有 1 条既有 ENOENT：clean checkout 缺少本机 ignore 的历史 fixture，已如实保留。
- **边界**: 阈值仍为 1；P0/P1、嵌入、向量空间、WS-2726/WS-3470、历史 assertion output 与生产 checkout 均未改。
