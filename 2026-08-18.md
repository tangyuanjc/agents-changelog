## [Codex-CTO] WS-3831 · D 线机器告警退出 JC 私聊

- **时间**: 2026-08-18 08:25 上海。
- **文件**: `host-watchdog/multica_runtime_offline_alert.py`、`host-watchdog/multica-runtime-offline-alert.env.example`、`host-watchdog/test_multica_runtime_offline_alert.py`。
- **根因**: WS-3709 的 protected secrets 会把主配置中的空投递目标恢复成 JC open_id；该 open_id 同时是 allowlist 唯一成员，因此 WS-648 的“非白名单目标 file-only”闸按设计放行，未能表达“禁止机器状态进入个人私聊”的真实边界。
- **变更**: C 线即时告警与 warning digest 默认改为结构化落盘到 `~/.log/dline-alert-delivery.jsonl`；digest/canary 落盘后仍推进 delivery state，避免把“主动 file-only”误判成投递 stale。飞书投递改为显式 opt-in，并在代码层只接受 allowlist 内 `oc_` 群聊，任何 `ou_` 私聊目标均拒绝。
- **上线**: 通过 main 分支 `host-watchdog/install.sh` 的 pytest 闸安装，不直接修改 `~/.bin/` 副本；launchd 监控本身与 60 秒检测频率保持开启。

## [Opus-CSO] hr39 立宪 · 四合一工作法铁律 (第一性+对抗+奥卡姆+金字塔)

- **时间**: 2026-08-18 21:50 上海。
- **文件**: `AGENTS.md` (新增 hr39 一节, 插于「先核实再断言铁律」之后)。
- **内容**: 把 first-principles-adversarial skill v2 的四段骨架升为跨 agent 铁律 — ①第一性拆解 (承重假设/钱池归属) ②对抗式审查 (审查者≠起草者, 3±1 独立杀手) ③奥卡姆收敛 (禁静默吸收 kill, 禁砍验收闸) ④金字塔表达 (结论先行/数字单口径/默认 HTML)。判定边界: 重要方案/对外交付/验收裁决必走; 琐事降级三把刀; 对外不可逆永不豁免。
- **来源**: JC 2026-08-18 指令 (skill 升 v2 + 全组织分发 熵减 WS-3880~3897 / 四条人 WS-37~41 + GIRF 线全程用) → 依治理协议三动作决策树走三连。实证战绩: GIRF 盘账 8 路杀手修 32 处含 2 fatal / sector-radar 13 kill / 驱蚊合规拦截。
