# 2026-08-26

## [Opus-CSO] 宪法 v3.23 — hr41 符号链接锚 · 禁止沿软链接修改安装二进制

- **触发**: WS-4332 (父票 WS-4284)。8/26 凌晨 Herdr/Grok 事故技术层已修复, JC 要求由宪法唯一 writer 做跨-agent 规则层收口。
- **判断**: 走 **hr41 补丁**而非新立 hr42。理由 —— hr41 正文管「**你换的 binary 比你以为的活**」(磁盘 binary ≠ 运行态), 本次事故是同一病的另一半「**你编辑的路径比你以为的深**」(`~/.local/bin/<x>` 常是软链接, file-edit 沿链接穿透打到 installer 托管的真身)。同族合并读, 对齐 hr32 最小工程 + v3.21 元教训 (一条没落地的禁令, 加第二条禁令不会让它落地)。
- **规则要点**: ① 动 `bin/` 下任何可执行文件前必跑 `test -L` + `readlink`/`realpath` + `file` + `stat` 三件只读探查; ② 软链接与 installer-managed 路径 (`downloads/`、`versions/<ver>/`、Homebrew Cellar、`node_modules/.bin`、venv `bin/`) 禁 file-edit / `>` 重定向 / `tee` / `sed -i` / `cp` 就地覆盖 / `patch`; ③ 要加包装逻辑只准新建**独立 regular file wrapper** 放 PATH 靠前处 `exec` 真身。
- **明确不做 (防过纠)**: **不锁死 installer-managed 官方二进制** —— 官方 updater 照常可用, 禁止给官方二进制加 `uchg`, 禁止另起第二套 updater。**锁编辑动作, 不锁升级通道**; 升级通道归 hr41 正文, 两条互补不重叠。`uchg` 只加在我们自建的 wrapper 上。
- **覆盖面写实**: 规则适用本机全部 agent 与 Herdr pane 内任何 agent; **员工机只有能力分发落地后才算覆盖**, 未分发前按未覆盖计, 不许写成"全组织已生效"。
- **机械兜底缺口 (据实记录, 不粉饰)**: 实测只有 Grok 一家有守卫 (`~/.grok/hooks/protect-install-binaries.sh` PreToolUse fail-closed + config `[permission] deny` 三条)。**Codex / Claude Code / Kimi 的 PreToolUse 钩子全是遥测与 herdr 状态上报, 无同类保护; Cursor 无 hooks 机制。** 且该守卫写死 3 条 Grok 专用路径, 而 `~/.local/bin` 下实测 **21 条软链接 launcher** (cursor-agent / codex / hermes / node / npm / npx / openclaw / wechat-cli / xiaohongshu-mcp / marvis / gbrain 等) 全部裸奔 → **路径枚举法注定补不全, 故本锚写成行为通则而非黑名单**。泛化票 **WS-4333** (backlog), 未补齐前不得声称机械覆盖。
- **同步纠正旧真源**: memory `feedback_herdr_agents_full_access_0809.md` 就地改正 + 加勘误块 —— Grok 全局交互默认**不再是** `always-approve`+`yolo=true`, 实测现为 `permission_mode="auto"` + `yolo=false`; 真正无人值守只准**显式、窄作用域**开 always-approve。理由同 hr41「同步作废」条: agent 上手时读的是那份文档不是宪法。
- **根因口径**: 8/12 起的 `yolo`+`always-approve` 是**风险放大器不是根因**; 直接根因 = agent 没识别 launcher 是软链接, 编辑沿链接穿透截断 installer 托管的二进制 (installer 版本记录仍以为完好 → 静默变砖)。
- **边界**: 未改任何 Grok/Herdr 运行配置 (那是执行面, 已由技术层完成); 未新建 autopilot / 常驻 loop (对齐 WS-3353 三禁); 未写入任何凭证值或会话正文。

## [Opus-CSO] 补记 — 宪法 v3.22 (hr41 正文) 的 changelog 缺漏回填

- v3.22 (hr41 `live daemon 换 binary = 不可逆生产动作`) 已于 8/26 凌晨写入宪法并提交 (`~/.org` commit `9f6f72a`), 但**三连的 changelog 一条当时漏写**, 本日补记。
- 要点: 存在 live Herdr server 时禁 `brew upgrade herdr` / 裸 `brew upgrade` / `--greedy` / `brew reinstall` / `brew services restart|stop herdr` / 覆盖 `/opt/homebrew/bin/herdr`; 唯一合规路径 = 存快照 → 清场 → `herdr update --handoff` → 四闸机器验收 (server/client version+protocol 相等、`compatible: yes`、快照按稳定外号逐条对账、原会话仍能 `herdr agent read` 读到内容)。`agent_status` 两个方向都不单独采信。机械兜底 `brew pin` 已落地。
- 实证: brew 于 8/25 23:45 (上海) 把 Cellar 换成 0.8.2/protocol 20, 运行中 server/client 仍 protocol 19 → 控制面 `protocol_mismatch`; 约 00:23 server 重启, 全部旧 pane 收 SIGHUP, 本机 9 个 live pane 进程结束。

## [Opus-CSO] 宪法 v3.22 — hr41 · live daemon 换 binary = 不可逆生产动作 (Herdr 更新只走 handoff)

- **时间**: 2026-08-26 01:5x 上海。驱动票 WS-4284 (JC 立项), 派生 WS-4286 (backlog)。
- **事故**: brew 于 8/25 23:45 上海时间把 Cellar 换成 herdr 0.8.2/protocol 20 (Cellar 目录时间戳为凭), 运行中 server/client 仍 protocol 19 → 控制面 `protocol_mismatch`; 约 8/26 00:23 server 重启, **按设计向全部旧 pane shell 发 SIGHUP, pane 内 agent 进程全部结束**。布局可恢复, 运行态不恢复。事发面 = 本机 9 个 live pane。
- **根因 (本次最重要的发现, 不在票面原始描述里)**: 动手的 agent **是在照做组织自己写的指引**。memory `project_session_0809_herdr_onboard_handoff.md` §③「更新」行原文写着 `brew upgrade herdr`（勿用 `herdr update`，那是 install.sh 通道）—— 0.8.0 时代正确, 0.8.2 时代致命。**且 `brew info herdr` 的 caveat 原文就写着「To restart herdr after an upgrade: `brew services restart herdr`」—— brew 自己推荐的这条正是杀 pane 的那条**。即事故是"照章办事"的产物, 不是违规的产物。
- **hr41 内容**: 通则 = **磁盘上的 binary ≠ 正在跑的运行态**; daemon 正持有别的 agent 的活时, 换 binary / 重启它 = 不可逆生产动作, 按「生产部署授权边界」办。Herdr 面按**效果**禁 (不按命令字面): `brew upgrade herdr` / **裸 `brew upgrade` 与 `--greedy`** / `brew reinstall` / **`brew services restart|stop`** / 覆盖 binary / 自创"先装再重启"。唯一合规路径四步 = 存快照 → 清场 → `herdr update --handoff` → 过四闸。完成门槛四闸机器可测 (对齐 hr37): server/client version+protocol 相等 · server `compatible: yes` · 与快照**按稳定外号 name 逐条对账** (pane_id 会变不能拿它对) · 原非空会话仍 `herdr agent read` 读得到内容。
- **对票面原始要求的两处 CSO 收敛 (已在正文标注)**:
  - **票面只点名禁 `brew upgrade herdr`; 收敛为按效果定义** —— 裸 `brew upgrade`(不带包名) 会连带升 herdr, 是最容易漏的入口; brew caveat 自荐的 `brew services restart herdr` 同样致命。只禁字面命令挡不住这两条。
  - **票面的"完成门槛"是纯验收态; 补了前置「清场」** —— 快照里任一 pane `agent_status=working` 先通知该 agent 按 HANDOFF 协议 checkpoint 落盘再升。**且明确写成"不是永久禁升"**: 对抗审查指出"live server 常驻 + 常有 working pane"会让规则自锁死、herdr 永远升不了级, 故给出停机窗口逃生口 (票面写窗口 + 通知 pane 主, 走生产部署授权边界)。
  - **`agent_status` 双向都不单独采信**: 票面只要求 `unknown` 不算死; 补上 `idle` 也不算"没活在跑" (清场以 agent 自己确认 checkpoint 为准) —— 实测 9 pane 有 4 个带 `screen_detection_skipped`, 该字段是屏幕检测推断值。
- **机械兜底 (已落地, 非计划)**: `brew pin herdr` **已执行**, 现 pinned = `herdr` + `multica` (multica 早已 pin, 同类"live daemon 被 blanket upgrade 顺手打死"的病, 有先例)。pin 后裸 `brew upgrade` 不再顺手升它。已验证 pin 是纯元数据操作, 未扰动运行中 server (PID 9213 存活, `compatible: yes`)。
- **落地手段刻意不止于禁令文字**: 直接对齐 v3.21 元教训「一条一个月没落地的禁令, 加第二条禁令不会让它落地」。故三件同时做 —— ① 宪法立条 ② **就地改写 0809 上手文档那一行**(agent 上手 Herdr 真正会读的是它, 不是宪法) ③ **brew pin 机械闸**(不依赖任何 agent 的记性)。
- **对抗审查 (hr39, 审查者≠起草者, 两轮独立)**: 第一轮杀掉草案 —— 草案把"已改文档 / 已 pin"写成既成事实而实际都没做 (**在宪法里写假事实 = 负价值**), 且规则自锁死无逃生口。第二轮对最终稿逐条实测证伪, 又抓到 **3 处硬错**: (a) 步骤 1 写的 `herdr agent list --output json` **这个 flag 不存在**, 照抄会 exit 2 写出 0 字节空快照, 第 3 闸变成拿空文件对账 = 等于没验 (正解: 裸 `herdr agent list` 本身就吐 JSON, 已实测 4813 字节); (b) `screen_detection_skipped` 实为 4/9 我写成 5/9; (c) `herdr agent prompt` 的 TEXT 是必填位置参数。三处均已改, 并把 (a) 的坑显式写进正文当护栏。
- **留的口子 (未实测, 不许写成已解决)**: `herdr update` 走 install.sh 通道不走 brew, 跑完之后 brew 记录与磁盘是否失去一致 (会不会被下次 `brew upgrade`/`reinstall` 打回旧版) **尚未实测** → 开 WS-4286 收, **状态刻意设 backlog 而非 todo**: todo 会立刻拉起 Codex run, 而这题的自然实验方式就是跑一次 `herdr update`, 在 live server 上跑 = 事故重演。票内已写硬约束"禁在 live server 实测"。
- **边界**: 只改宪法/治理文字 + 那一行误导文档 + brew pin 元数据; **未动任何 Herdr/Grok 配置, 未碰 `~/.config/herdr/`, 未重启任何服务, 未 unpin**。票面边界要求"不改 Herdr/Grok 配置" —— `brew pin` 判为 Homebrew 包元数据而非 Herdr 配置, 且实证不触及运行态, 故在边界内执行。
- **顺带实测发现 (未处理, 记录备查)**: `brew services list` 里 herdr 显示 `error`, LaunchAgent `homebrew.mxcl.herdr.plist` 未托管成功; 当前 server 是独立进程 (PID 9213) 在跑, 即**它死了没有任何东西会把它拉起来**。属现存风险, 不在本票边界内, 未动。
