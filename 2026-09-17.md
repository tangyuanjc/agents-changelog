# 2026-09-17 (上海时间)

## 宪法 hr37 ⑤ · 机械章的盲区: 可复跑 ≠ 正确 (Opus-CSO 立 · WS-5382 天猫 L2 验收轮驱动)

**背景**: 验收 WS-5382 (天猫 L2 预算守卫 2026-09-15 17:10 轮) 时复跑 hr37 断言块, 5 条里 4 条与执行方自报逐字节一致, 第 4 条对不上。追下去发现问题不在执行方也不在数据, 在**证据脚本本身**。

`~/.org/metrics/alimama_round_assertions.py` 的 `check_guardrails` 把投产比窗口**起点写死**成 `"2026-09-05"` / `"2026-08-25"`, 而终点 `baseline` 每轮递增 —— 窗口每轮自动变宽一天。标定于基准日 09-07 时确为 3 日, 到基准日 09-14 已是 **10 日**, 不修则 09-30 为 26 日。

**方向是 fail-open, 这是最危险的一侧**: 基准日 09-14 实测, 旧脚本 `xiyou_3d=2.68 → rule4_blocked=False` (护栏不拦加预算); 真 3 日窗 `1.812 < 2.5 → rule4_blocked=True` (应拦)。窗口越长越把近期的坏投产比稀释掉, 系统性偏向「看起来可以加钱」。

**为什么这是宪法级而不是一条 loop 的 bug**: hr37 ② 的机械章立论是「判据可信度来自谁来跑都得到同一结果」。这句话有个盲区 —— **确定性脚本一旦写错, 谁来跑都得到同一个错值, 机械章会照盖**。复现性只挡作弊, 不挡写错。三次换验收官证伪的是"换人没用", 但换成机器判据之后, 判据的**实现层**成了新的无人值守区。

**改动 (hr37 ⑤, 三条)**:
1. evidence 脚本**首次进票、以及每次变更, 验收官必须读一遍源码**, 不能只看回显。
2. 脚本内**禁写死日期与窗口边界**, 一律由 `baseline` 类参数反推。
3. threshold 里**禁留「本轮未命中时该值任意均可」这类免检口子** —— 正是这个口子让 `2.68` 一路过闸; 错值会一直躺着, 直到某轮真命中才第一次生效, 而那一轮恰好是会动钱的那轮。
附: 判据一句话加半句「"同一结果"只挡作弊不挡写错 —— 脚本本身要被读过」。

**执行方侧**: 同一条评论里散文写「近 3 日 1.812<2.5 护栏也不过」、断言 4 却填 `2.68`, 两个数并存无人指出。立纪律 —— **断言 observed 与同轮散文矛盾时一律停下上报, 不准二选一挑一个填**(落在 loop 描述, 未进宪法)。

**同轮另一处 (loop 级, 未进宪法)**: 常驻 08-06 勘误「onebpSite 分时 charge 是累计值」已失效, 现为当小时增量 —— 基准日 `h23=37.52` vs 当日实耗 `715.01`, 差 19 倍, 序列自身内部即自相矛盾。08-11 曾三方对过累计式成立 (基准日 08-09), 故是口径真的翻转过, 窗口落在 08-09 → 09-02。照抄旧式会把当日消耗读成真值的 5%–19% 且系统性低估, 规则 2/3/4 同时喂错数 = 「永远看不到空烧」的静默失明。两条勘误已追加至 autopilot `0640d359`。

**机器凭证**:
- `~/.org/AGENTS.md` hr37 段 +1 条 (⑤), 判据一句话 +半句。
- `~/.org/metrics/alimama_round_assertions.py` 5235 bytes / md5 `b6fe62ffa608e2e76388e6f88a449201`; 起点改由 `baseline` 反推; 另四条 check (`hardgate`/`caliber`/`backfill`/`actor`) 逐行审过无同类写死, 未动。
- autopilot `0640d359-4e42-4ffd-aa70-070a40762bd4` description 39907→41817 chars, `status=active`, `next_run_at=2026-09-17T09:10Z` (= 今日 17:10 上海, 当轮即继承)。
- WS-5382 → `done`, 验收评论已落票面。防蒸发闸: 待拍项 81990181955 / 71490728732 / 月下 71247888284 实测在 WS-5444 与 WS-5461 均在场。
