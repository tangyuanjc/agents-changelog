## [Codex-CTO] 文雅/小龙日常员工语料回拉闭环

- **时间**: 2026-08-19 07:42 上海。
- **接入**: 文雅 `10.20.20.3` 与小龙 `10.20.20.10` 已用既有非管理员身份、chroot、restrict 只读 SFTP、专钥/known-host 路由进入五 peer 清单；launchd 固定 1800 秒。
- **两轮**: 第一轮两机均 `ok=true`；第二轮由 launchd 自然触发，文雅 validated 1788、小龙 37292，两机再次 `ok=true`。总 exit 1 只来自泡泡离线。
- **总账**: active + `events-archive/*.jsonl.gz` 按 request_id 去重为文雅 1788、小龙 37292；active 回落是 retention 归档，不是 external 覆盖丢写。
- **并发修复**: 中央 `capture.lock` 改为持久 0600 inode并校验 named/opened inode，关闭旧 unlink-before-unlock 的 split-inode 风险；CPA 全量 273/273、员工 importer 5/5。
- **边界**: 官方任务保持 Disabled，Silent 为活任务；未跑 start/start2，未改 Windows 账号，未碰芳芳 `.5`/WS-2957，未评 WS-2958，未读 raw 正文/`~/.colima`，未联系同事。
- **条件分支**: 黄宁 `.4`、欣欣 `.11` 均不在组网且 22 不通，本轮未接入。

## [Codex-CTO] 修复 Loop-1 记忆双轴仪表假红

- **时间**: 2026-08-19 09:16 上海。
- **P0**: WS-3903 把 `gbrain_pages_daily_growth` 的 reports / non-reports 都改为从 `list_pages updated_after` 的 slug 同源分类，不再用 Pages 净增减 reports 更新数；2026-08-19 实数 43 reports + 5 non-reports 回归 PASS，2026-08-18 零 non-reports 仍 FAIL，reports 纯更新不再产生负增长。
- **P1**: 陈旧窗口携带上日 FAIL 时，当日仍保留未解决 FAIL 判定，但同时原子 rebaseline，下一个 24h 窗口可重新实测，关闭无限 carry 不动点。
- **P2**: GBrain 员工×板块查询首次 `eligible_candidate_count=0` 时先原查询重试一次，再进入现有 employee-fact anchor fallback；样本新增 retry used / recovered / attempts 证据。
- **验证**: blackboard consumer assertions 56/56 PASS；memory-axis health 31/31 PASS。`output/assertions/2026-08-19.json`、嵌入模型和向量空间未改。
- **交付**: blackboard-v3 本地生产 `main` 已前进到 `7b156f0` (仓库无 remote)；`.org` 语义重试为 commit `15a381f`、PR #74，待 review/merge，未直接覆盖原 main 中其他未提交改动。
