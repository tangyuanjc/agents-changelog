# 2026-08-04

## [2026-08-04 00:08 CST] [Opus-CSO] [type:ops] WS-2963 亲验三仓分支保护 + 加固三个真相仓 (integrity-only 形态)

- 亲验背景: Grok 对抗官 8/3 配完 data-pipelines / erp_agent_plan / sector-radar 三仓 main 分支保护并自报生效。按 10/80/10 跨血统原则 (执行者 ≠ 验收者), CSO 亲跑复验, 不采信自报。
- assert_1 PASS: 三仓均 `enforce_admins=true` / `allow_force_pushes=false` / `allow_deletions=false` / `required_approving_review_count=0` (0 approvals 属单人仓预期, 不算失败)。
- assert_2 PASS: 强推对抗刀 (main → 父提交 + force) 三仓均返回 HTTP 422 `Changes must be made through a pull request. Cannot force-push to this branch`, 事后回读 main SHA 全部未变, 无需恢复。
- assert_3 PASS: 8/3 CSO 合入的三个 `Merge PR#1` 提交仍在 origin/main 顶端 (API + 本地 git 双证)。
- 超出 Grok 自报的补充对抗刀: ① 无 PR 直接建文件提交 (contents API) 三仓均 409 拦截 ② 真实 git 协议推送 (无本地 hook 的干净 clone) 普通推送与强推均被服务端 GH006 拒绝 —— 这是 agent 日常真正会走的路径, 比 API 探测更贴近实战。
- **sharp edge (硬坑) 立此存照**: `git push --dry-run` 对受保护分支给出**假通过** —— 同一个提交 dry-run 报 `6682796..2b048d5 main -> main` 成功, 真实推送被 GH006 拒绝。dry-run 不评估服务端分支保护, **任何人不得用它验证保护是否生效**。
- 加固决策 (CSO 自主权范围内, 全库 21 仓仅 3 仓受保护, 宪法仓 org-constitution 当时裸奔): 对三个"真相仓" org-constitution / agents-changelog / agent-memory 施加 **integrity-only (仅完整性)** 形态 —— `enforce_admins=true` + `allow_force_pushes=false` + `allow_deletions=false`, 但**不要求 PR**。
- 为何不套用 Grok 的 PR-required 形态: 那会打断 CSO 三连 (AGENTS.md 改 + changelog + push) 和 agent-memory 每日备份 cron 的直推路径。integrity-only 挡的是**不可恢复的灾难** (改写历史 / 删分支), 放行的是**可追溯的日常直推**, 成本零、收益满。
- 施加前已核实无自动化依赖强推: `~/bin/agent-memory-backup.sh:36` 走的是 `git push origin main` (普通推送), `~/.org` 全域仅一处 `--force-with-lease` 且作用于 feature 分支, 不影响 main。
- 施加后复验: 三仓强推均 422 `Cannot force-push to this branch`, 删分支 422, SHA 全未变; 本条 changelog 的推送本身就是**直推仍可用的实测证据** (dry-run 不可信, 故用真实推送验证)。
- 未覆盖面 (CSO 判定不需保护, 非待办): 其余 15 个项目 / 实验仓 (supply-sonar / opus-tasks / hermes-* / wechat-* 等) 价值密度低, 保持裸奔; org-context-mirror 因镜像语义可能合法强推, 一并排除。
- 已知边界 (非缺陷): admin 自己合自己的 PR 仍可以 (单人仓必要, 0 approvals 即此意); 本地 pre-push hook 加 `--no-verify` 仍可绕过本机闸, 但推到 GitHub main 由服务端拦截兜底; 删分支返回的 422 文案是 `Cannot delete the default branch` (GitHub 默认分支语义), 故 `allow_deletions=false` 本身未被独立证伪或证实, 对非默认分支才有独立意义。
